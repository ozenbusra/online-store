<h1><%= @item.item_name %></h1>

<h2>Stores</h1>

<%= render :partial => 'shared/store_items_index', store_items: @store_items %>

<p> <%= link_to "Add to Store", new_store_item_path(item_id: @item.id) %> </p>